# repo-setup 저장소 설정. 완료 게이트가 이 명령으로 검사한다.
# 형식은 한 줄에 키 하나다. macOS 기본 파이썬(3.9)에 tomllib이 없어 온전한 TOML 파서를 쓰지 않는다.
fast_test_command = "tests/guard/unit.sh"
test_command = "tests/guard/unit.sh && tests/setup/unit.sh && tests/license/unit.sh && tests/invariants.sh"
